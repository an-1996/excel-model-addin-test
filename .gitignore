# Node dependencies
node_modules/

# Optional: Build directories (but we WANT dist)
# /dist  <- Make sure this line is NOT present or is commented out

# Log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Optional: Operating system files
.DS_Store
Thumbs.db